[project]
name = "hospital-management-system"
version = "0.1.0"
description = "A Hospital Management System web application"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "flask",
    "flask-sqlalchemy",
    "flask-login",
    "werkzeug"
]

